@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<!DOCTYPE html>
<html lang="bg">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Horizons</title>

    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />

    <style>
        body {
            background: #f2f7f5;
        }

        header.navbar, nav.navbar {
            background: #247158 !important;
            padding: 0.7rem 1.5rem;
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.15);
        }

        .logo-img {
            height: 45px;
            width: auto;
            object-fit: contain;
            margin-right: 12px;
            border-radius: 50%;
        }

        .logo-link {
            display: flex;
            align-items: center;
        }

        .navbar-brand,
        .nav-link,
        .dropdown-item {
            color: #fff !important;
            font-weight: 500;
            font-size: 1.05rem;
        }

            .nav-link:hover,
            .navbar-brand:hover {
                opacity: .85;
            }

        /* ✅ Финален, единствен и работещ стил за бутона */
        .btn-add-destination {
            display: inline-block;
            padding: 10px 20px;
            background: linear-gradient(135deg, #2196f3, #1e88e5);
            color: white !important;
            font-weight: 600;
            border-radius: 12px;
            text-decoration: none;
            font-size: 1rem;
            margin-left: 15px;
            box-shadow: 0 4px 14px rgba(33,150,243,0.35);
            transition: 0.25s ease;
        }

            .btn-add-destination:hover {
                background: linear-gradient(135deg, #1e88e5, #1976d2);
                box-shadow: 0 6px 20px rgba(25,118,210,0.45);
                transform: translateY(-2px);
            }

        .btn-logout {
            background: #ffcc00;
            color: #4b3300;
            border: none;
            font-weight: 600;
            padding: 6px 16px;
            border-radius: 8px;
        }

            .btn-logout:hover {
                background: #e6b800;
            }
    </style>

    @RenderSection("Styles", required: false)
</head>

<body>

    <header>
        <nav class="navbar navbar-expand-lg navbar-dark">
            <a class="navbar-brand" asp-controller="Home" asp-action="Index">Horizons</a>

            <button class="navbar-toggler" type="button"
                    data-bs-toggle="collapse" data-bs-target="#navMenu">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navMenu">

                <ul class="navbar-nav me-auto">

                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Destination" asp-action="Index">Дестинации</a>
                    </li>

                    <a asp-controller="Home" asp-action="Index" class="logo-link">
                        <img src="~/images/logo.jpg" class="logo-img" alt="Сайт Лого" />
                    </a>

                    @if (SignInManager.IsSignedIn(User))
                    {
                        if (User.Identity!.Name == "admin@horizons.com")
                        {
                            <!-- 🔥 Красив бутон "Добави дестинация" -->
                            <li class="nav-item">
                                <a class="btn-add-destination"
                                   asp-controller="Destination"
                                   asp-action="Add">
                                    ➕ Добави дестинация
                                </a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Reservation" asp-action="Admin">
                                    🛠 Всички резервации
                                </a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Rating" asp-action="All">
                                    ⭐ Оценки & Коментари
                                </a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Destination" asp-action="Favorites">Любими</a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Reservation" asp-action="My">
                                    Моите резервации
                                </a>
                            </li>
                        }
                    }
                </ul>

                <ul class="navbar-nav ms-auto">

                    @if (SignInManager.IsSignedIn(User))
                    {
                        <li class="nav-item">
                            <span class="nav-link">👤 @User.Identity!.Name</span>
                        </li>

                        <li class="nav-item">
                            <form method="post"
                                  asp-area="Identity"
                                  asp-page="/Account/Logout"
                                  asp-route-returnUrl="@Url.Action("Index", "Home")">
                                <button class="btn btn-logout btn-sm">Изход</button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-area="Identity" asp-page="/Account/Login">Вход</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Регистрация</a>
                        </li>
                    }

                </ul>

            </div>
        </nav>
    </header>

    <div class="container mt-4 mb-4">
        @RenderBody()
    </div>

    <footer class="text-center text-muted mt-5 py-3">
        © @DateTime.Now.Year - Horizons
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    @RenderSection("Scripts", required: false)

</body>
</html>
